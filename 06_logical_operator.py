temp = int(input("What is the temp outside? "))

if temp >=0 and temp <=30:
	print("The temp is good today!")
	print("Go outside!")
else:
	print("The temp is bad today!")
	print("stay inside!")


#and or not || and: means both should be right || or : any one should be true || not : just reverse the situation